# plant-care

観葉植物のお世話をサポートする個人用Webアプリ。
水のやりすぎを防ぎ、植物の種類に応じた適切なケアを支援する。

## 開発者プロフィール

- プログラミングほぼ未経験（HTMLを少し触ったことがある程度）
- macOSで開発、iPhoneのSafariで使用
- 複雑な技術選択より、理解しやすさ・確実な動作を優先する

このプロジェクトでは、**素直で読みやすいコード**を選ぶこと。
理由は次の2つ：

1. 開発者は未経験のため、後から修正を依頼するときに
   AI（Claude Code含む）が該当箇所を見つけやすい構造である方が、
   修正の精度と速度が上がる
2. 過度な抽象化や凝った書き方は、未経験者プロジェクトでは
   バグの温床になりやすい

ただし、これは「機能を制限する」という意味ではない。
**実現したい機能はしっかり実装した上で、その実装を素直に書く**こと。

開発者自身がコードを読めることは目的ではない。
コードの構造が「次の修正がしやすい状態」であることが目的。

## プロジェクトの目的

開発者は過去に以下の理由で観葉植物を枯らしている：

1. 水のあげすぎによる根腐れ
2. 置き場所と植物の相性のミスマッチ
3. 植物の種類に応じたお世話の知識不足

このアプリは上記3つの課題を、シンプルなWeb UIで解決する。

## 主な機能（最小バージョン）

### 1. 植物カードの登録・一覧表示
- 種類（事前データベースから選択）、ニックネーム、置き場所、迎えた日を入力
- 一覧画面で全植物のカードを確認できる
- 1〜3鉢程度を想定（少数前提でOK）

### 2. 水やり判定
- 「水やりしてOK?」を植物ごとに判定
- 最後の水やり日からの経過日数 × 季節別の推奨間隔 で判定
- 「まだ早い／そろそろOK／遅れ気味」の3段階で表示
- 水やりを記録するボタンで履歴を残す

### 3. 困ったときの相談（トラブル辞書）
- 植物カードから「困ったときは」ボタンで入る
- 症状一覧と詳細を同時に表示する（一覧は常に見える）
  - 症状を選ぶと詳細（原因・対処法）が一覧の下に表示される
  - 別の症状を選ぶと詳細がその場で切り替わる
  - 選択中の症状ボタンは色が変わり、どれを見ているか分かる
- 症状一覧は植物に応じて表示順を最適化（その植物に多いトラブルを上に）

### 4. データのエクスポート/インポート
- 全データを1つのJSONファイルとしてダウンロード（バックアップ）
- JSONファイルからデータを復元できる
- 復元実行前に confirm() で確認ダイアログを表示する（誤操作防止）
- 機種変更やSafariデータ消失への備えとして必須機能

## 技術スタック

- **言語**: HTML / CSS / JavaScript（バニラJS、フレームワーク不使用）
- **データ保存**: ブラウザのLocalStorage
- **植物図鑑データ**: data/plants.json（事前定義）
- **トラブル辞書**: data/troubles.json（事前定義）
- **ビルドツール**: 不使用（index.htmlを直接ブラウザで開いて動く構成）
- **依存ライブラリ**: なし（CDN経由含めゼロを目指す）

## ディレクトリ構成

```
plant-care/
├── CLAUDE.md
├── README.md
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── app.js
│   ├── storage.js
│   ├── watering.js
│   └── ui.js
├── data/
│   ├── plants.json
│   └── troubles.json
└── assets/
    └── icons/
```

## ファイルごとの役割

- **index.html**: 単一ページ。画面切り替えはJavaScriptで制御
- **css/style.css**: 見た目。可愛さを意識したデザイン（後述）
- **js/app.js**: アプリ起動時の初期化、画面遷移の制御
- **js/storage.js**: LocalStorageへの保存・読み込み、エクスポート/インポート
- **js/watering.js**: 水やり判定の純粋なロジック（DOM操作を含めない）
- **js/ui.js**: 画面の描画、イベントハンドラ
- **data/plants.json**: 植物図鑑（30種類程度、ただし最初は5種類の仮データから）
- **data/troubles.json**: トラブル辞書（20症状程度、ただし最初は5症状から）

## データスキーマ

### ユーザーが登録する植物（LocalStorageに保存）

```json
{
  "id": "plant-001",
  "nickname": "パッキー",
  "species": "pakira",
  "placedAt": "リビング東窓",
  "addedDate": "2026-04-30",
  "lastWatered": "2026-04-28",
  "wateringHistory": [
    { "date": "2026-04-28", "note": "" }
  ],
  "photos": [],
  "memo": ""
}
```

### 植物図鑑データ（plants.json）

```json
{
  "id": "pakira",
  "displayName": "パキラ",
  "wateringInterval": {
    "spring": 7,
    "summer": 5,
    "autumn": 7,
    "winter": 14
  },
  "light": "明るい日陰",
  "difficulty": "易しい",
  "description": "幹がぷっくりした人気の観葉植物。乾燥に強い。",
  "commonTroubles": ["leaf-yellow", "leaf-tip-brown", "leaf-drop"],
  "sources": [
    "情報源URL or 書籍名 or 園芸店での聞き取り（日付）"
  ]
}
```

### トラブル辞書（troubles.json）

```json
{
  "id": "leaf-yellow",
  "symptom": "葉が黄色くなった",
  "possibleCauses": [
    "水のやりすぎで根腐れ",
    "肥料不足",
    "古い葉の自然な変化"
  ],
  "actions": [
    "土の湿り具合を確認、ジメジメしていたら数日水を控える",
    "鉢底から水が抜けているか確認",
    "下葉だけなら自然な代謝なので様子見でOK"
  ]
}
```

## 水やり判定のルール

```
入力: 植物ID, 最後の水やり日, 今日の日付
出力: "early" | "ok" | "late" + メッセージ

1. 今日の月から季節を判定
   - 春: 3, 4, 5月
   - 夏: 6, 7, 8月
   - 秋: 9, 10, 11月
   - 冬: 12, 1, 2月
2. plants.jsonから該当植物の季節別水やり間隔を取得
3. 経過日数 = 今日 - 最後の水やり日
4. 判定:
   - 経過日数 < 推奨間隔 - 2 → "early"（まだ早い、あとN日待とう）
   - 推奨間隔 - 2 ≤ 経過日数 ≤ 推奨間隔 + 2 → "ok"（そろそろOK）
   - 経過日数 > 推奨間隔 + 2 → "late"（少し遅れ気味）
```

注意：この判定はあくまで目安で、「最終判断は土の乾き具合を見ること」を
UIに明記する。アプリの判定が絶対と誤解させない。

## 命名規則

### ファイル名・フォルダ名
- すべて小文字、単語間はハイフン: `watering-judge.js` ❌ 使わない、`watering.js` ✅

### JavaScript変数・関数名
- camelCase: `lastWatered`, `judgeWatering()`
- 定数は UPPER_SNAKE_CASE: `MAX_PLANTS = 50`

### CSS クラス名
- kebab-case: `.plant-card`, `.watering-status`

### HTML id
- kebab-case: `id="plant-list"`

## エラー処理方針

- ユーザーの操作ミス（入力漏れなど）は、画面上に分かりやすいメッセージで返す
- LocalStorageの読み書き失敗は try-catch で捕捉し、ユーザーに知らせる
- 致命的なエラーで動かなくなる状況は避ける。最低限「再読み込みしてください」を出す
- console.error で開発者向けの詳細ログも残す

## プライバシー・データ方針

- すべてのデータはユーザーのブラウザ内（LocalStorage）に保存
- 外部サーバーへの送信は一切行わない
- 解析ツール（Google Analyticsなど）も入れない
- 写真も外部にアップロードせず、Base64でLocalStorageに保存（容量に注意）

## デザイン方針

開発者は「植物のイラストや成長記録が可愛い」見た目を重視している。

- **配色**: 緑を基調とした優しい色合い
- **アイコン**: 絵文字（🌱🪴🌿）または simple な SVG
- **フォント**: 丸みのある読みやすいフォント
- **レスポンシブ**: スマホ縦向きを最優先（iPhone Safari）
- **アニメーション**: 控えめに、操作のフィードバック程度
- **ホーム画面追加**: PWA対応（manifest.json、apple-touch-icon を用意）

## 開発の進め方（重要）

### Plan-First の原則

機能を実装する前に、必ず以下を出力すること：

1. やることのリスト（ファイル単位、関数単位）
2. 既存コードへの影響
3. ユーザーの確認を待つ

開発者は未経験のため、いきなりコードが大量に出てくると判断できない。
**「次に何をするか」を文章で説明し、承認を得てから書き始める**こと。

### 段階承認

以下のフェーズに分けて、各フェーズの完了時にユーザーに動作確認を求める：

1. **フェーズ1: 骨組み**（完了）
   - index.html、空のCSS、空のJSファイル
   - 「Hello, plant-care」が表示できる状態
2. **フェーズ2: データ準備**（完了）
   - plants.json に5種類くらいの仮データ
   - troubles.json に5症状くらいの仮データ
   - storage.js のLocalStorage読み書き
3. **フェーズ3: 植物の登録・一覧**（完了）
   - 植物追加フォーム
   - 一覧画面
   - LocalStorageに保存される
4. **フェーズ4: 水やり判定**（完了）
   - watering.js の判定ロジック
   - 一覧画面に判定結果を表示
   - 水やり記録ボタン
5. **フェーズ5: トラブル相談**（完了）
   - 植物カードから「困ったときは」ボタン
   - 症状一覧と詳細の同時表示（症状を選ぶと下に詳細が出る、選択中ボタンを強調）
6. **フェーズ6: エクスポート/インポート**（完了）
   - JSON出力ボタン
   - JSON取り込みボタン（復元前に confirm() 確認あり）
7. **フェーズ7: デザイン仕上げ**（完了）
   - 可愛い見た目への調整
   - ホーム画面追加（PWA）対応
8. **フェーズ8: 本格データ作成**（完了）
   - claude.ai での情報収集を経て、plants.json を充実（6種類）
   - troubles.json も充実（12症状）
   - 「辞書データの品質方針」セクションに従う
9. **フェーズ9: 水やり履歴カレンダー**
   - 第一段階（完了）: 今月のカレンダー表示・💧マーク・今日の強調
   - 第二段階（完了）: 「←」「→」ボタンによる月の切り替え
   - 第三段階（未着手）: 見た目の調整（必要に応じて）
   - 第四段階（完了）: 水やり記録の編集機能（カレンダーセルタップで追加・削除）
10. **追加実装**（完了）: 植物カードに「最後の水やり」日付テキストを表示

### 質問・相談のスタンス

- 仕様で迷ったら、勝手に決めずに**ユーザーに2〜3案を提示して選んでもらう**
- 推奨案とその理由を明示する
- 「とりあえずこうしました」は避ける

### コードに含めない要素

以下は明示的に**禁止**：

- 外部CDNからのライブラリ読み込み（jQueryなど含む）
- npm/yarn パッケージのインストール
- React、Vue、Svelte などのフレームワーク
- TypeScript（純粋なJSのみ）
- ビルド処理（webpack、vite など）
- バックエンドサーバーの追加
- データの外部送信

## 辞書データ（plants.json, troubles.json）の品質方針

辞書データの正確性はこのアプリの価値の根幹である。
誤った情報が登録されると「枯らさないアプリ」が「枯らすアプリ」になる。

このため：

1. **Claude Code単独で辞書を一括生成しない**
   Claude Code の知識だけで30種類を一気に作るのは禁止。
   思い込みや誤りが混入するリスクが高い。

2. **辞書データの作成は2段階で行う**
   - **情報収集フェーズ**：claude.ai（Webブラウザ版)で
     Web検索を活用して、複数の信頼できる情報源から
     植物データを収集する。情報源URLを必ず記録する。
   - **辞書化フェーズ**：収集した情報を Claude Code が
     JSON形式に整形する。Claude Code は数値や情報を
     勝手に補完・変更しないこと。

3. **数値（水やり間隔の日数など）の採用基準**
   - 複数の信頼できる情報源で値が一致するもののみ採用
   - 情報源によって違いがある場合は、保守的な値（より長い間隔）を採用
   - 不明な場合は「要確認」とマーク

4. **少数精鋭で始める**
   開発初期は5種類程度の仮データで動作確認する。
   開発フェーズ8で本格的な辞書作成を行う。
   実用段階では「実際に育てる植物だけ」を1種類ずつ追加していく。

5. **plants.json には情報源を必ず記録する**
   各エントリに `sources` フィールドを持たせ、
   参照したURL・書籍・園芸店での聞き取りなどを記録する。

6. **ユーザー（開発者）による最終確認**
   園芸店で植物を買うときに、店員から得た情報で
   辞書を最終調整する。これが最終的な品質保証となる。

### トラブル辞書データの品質方針

- 各症状について、考えられる原因（possibleCauses）と対処法（actions）を3つずつ書く
- 一般的な症状の対処法を扱い、植物別の個別対処は扱わない（拡張案2で対応）
- 原因は「水のあげすぎ」「日光不足」など、初心者にも分かる言葉で書く
- 対処法は「まず○○を確認する」「○○の場合は△△する」のように行動可能な指示で書く
- 専門的な薬剤名は原則使わず、「市販の殺虫剤」のように一般化する
- 原因は重要度順（最も多い原因が先頭）
- 対処法は実施しやすい順（簡単・無料 → 手間がかかる）

## 動作確認の方法

開発中は以下で確認：

1. macOS で `index.html` をダブルクリックしてSafariで開く
2. デベロッパーツール（右クリック→検証）でコンソールエラーを確認
3. iPhoneで確認するときは、Macと同じWi-Fiで簡易サーバーを立てる
   （その手順は必要時にユーザーに案内する）

## 将来の拡張案

最小バージョン完成（フェーズ8まで）の後に検討する機能。
今は実装しない。

### 拡張案1: 水やり履歴カレンダー（フェーズ9で実装予定）

**目的**: 過去の水やり履歴を視覚化し、植物ごとの水やりパターンを確認できるようにする。「先月いつ水やりしたっけ？」に答えられるようにする。

**仕様**:
- 各植物カード内、「水やりした」ボタンの下、「困ったときは」ボタンの上にカレンダーを配置する
- カレンダーは月間グリッド形式（日曜始まり、7列）で常時表示する
- デフォルトで今月を表示する
- 「←先月」「来月→」ボタンで月を切り替えられる
- 水やりした日には💧絵文字を表示する
- 今日の日付は視覚的に強調する（実装の見た目は任せる）
- 水やり履歴がない月もカレンダー自体は表示する（💧マークを表示しないだけ）

**データ構造**:
- 既存の wateringHistory 配列をそのまま使用する（追加のデータ準備は不要）
- 各エントリの形式: { date: "YYYY-MM-DD", note: "" }

**段階的実装**:
1. 第一段階（実装済み）: 今月のカレンダーを表示し、水やり履歴を💧で表示する。今日の日付を視覚的に強調する
2. 第二段階: 「←先月」「来月→」ボタンで月の切り替えを実装する
3. 第三段階: 見た目の調整（必要に応じて）
4. 第四段階: 水やり記録の編集機能（追加・削除）

**第四段階の仕様**:
- カレンダーの日付セルをタップすると、確認ポップアップを表示する
- セルに💧がない日（過去または今日）：「○月○日に水やり記録を追加しますか？」と確認、追加する
- セルに💧がある日：「○月○日の水やり記録を削除しますか？」と確認、削除する
- 未来の日付セルはタップしても何も起きない（イベントを無効化）
- 「水やりした」ボタンは引き続きメイン動線として残す（カレンダーからの編集は補助動線）
- 確認ポップアップは追加・削除で同じ形式（「キャンセル」と「追加/削除」ボタン）
- データ操作: storage.js の wateringHistory 配列を直接編集する関数を新規追加（addWateringRecord(plantId, date), removeWateringRecord(plantId, date)）
- 編集後はカレンダーが即座に再描画される
- 「水やりした」ボタン押下時の lastWatered 更新ロジックとの整合性を保つ（最新の水やり日を反映）

**実装上の注意**:
- 既存の植物カードのレイアウトを壊さないように、カレンダー領域を独立したコンポーネント（div）として配置する
- 月切り替えの状態は、各植物カードごとに独立して持つ（一つのカードで先月を表示しても、他のカードには影響しない）
- 月初・月末の空きセル（前月・翌月の日付）は淡色で表示するか、空白にする（実装は任せる）
- カレンダーが追加されることで植物カードが縦に長くなるが、その点は許容する（後で必要なら折りたたみ式に変更可能）

**前提条件**:
- フェーズ8（本格データ作成）が完了していること
- 既存の wateringHistory データ構造が維持されていること

### 拡張案2: トラブル辞書の植物別対処法（フェーズ10想定）

**目的**: 同じ症状でも植物ごとに最適な対処法は異なる。
「葉が黄色」でもパキラとサボテンでは対処が変わる。
植物別の個別アドバイスを表示できるようにする。

**仕様**:
- 共通の原因・対処法（既存の actions）はそのまま残す
- 各症状に「植物別の追加アドバイス」フィールドを追加
- トラブル相談画面で、共通対処の下に「○○（植物名）特有のアドバイス」を表示
- 植物別アドバイスがない症状はこれまで通りの表示

**スキーマ拡張**:
既存の troubles.json を拡張:
```json
{
  "id": "leaf-yellow",
  "symptom": "葉が黄色くなった",
  "possibleCauses": [...],
  "actions": [...],
  "speciesSpecific": {
    "pakira": {
      "advice": ["パキラ特有のアドバイス..."]
    },
    "cactus": {
      "advice": ["サボテン特有のアドバイス..."]
    }
  }
}
```

**段階的実装**:
1. 第一段階: speciesSpecific フィールドの追加とUI表示
2. 第二段階: 育てている植物分の個別データ収集（claude.aiでの情報収集）
3. 第三段階: 残りの植物の個別データ収集

**実装上の注意**:
- 既存の troubles.json データは壊さない（後方互換性）
- speciesSpecific フィールドがない症状は、これまで通りの表示にフォールバック
- 個別データの作成は claude.ai で情報収集してから Claude Code に渡す方式（フェーズ8と同じ）

**前提条件**:
- 実際に植物を育てて、個別対処の気づきが集まっていること
- フェーズ8（本格データ作成）が完了していること

### 拡張案3: 植物情報の編集機能（フェーズ11で実装予定）

**目的**: 植物の登録時に間違えて入力した場合や、後から内容を修正したい場合に対応できるようにする。「リビング窓辺」に置いていた植物を「玄関」に移動した、ニックネームを変更したい、迎えた日を入力ミスした、などのケースに対応する。

**仕様**:
- 各植物カードの植物名（ニックネーム）の右端に「✏️ 編集」ボタンを配置する
- ボタンをタップすると、新規登録画面と同じ形式で別画面に遷移する
- 編集画面のタイトルは「植物を編集」とする
- フォームには現在の値が初期値として入力されている
- 編集できる項目: 種類、ニックネーム、置き場所、迎えた日（4項目すべて）
- 「更新する」ボタンで保存し、植物一覧画面に戻る
- 「← 戻る」ボタンで保存せずに植物一覧画面に戻る
- 既存の水やり履歴（wateringHistory）はそのまま保持する（編集による消失なし）

**仕様上の注意（種類変更時の挙動）**:
- 「種類」を変更した場合、その時点から新しい種類の水やり判定基準（plants.json の wateringInterval）が適用される
- 過去の水やり履歴の「日付」情報は保持されるが、判定ロジックは変更後の種類で計算される
- これは仕様として受け入れる（警告ダイアログは表示しない、シンプルな動作を優先）

**データ操作**:
- storage.js に updatePlant(plantId, updates) のような関数を新規追加
- updates は変更後の値（type, name, location, plantedDate）を持つオブジェクト
- 既存のid、wateringHistory、lastWatered は保持する

**UI 実装の考慮点**:
- 編集画面のフォームは、新規登録画面のフォームを再利用する（HTML、CSS、JSの共通化）
- 「新規登録モード」と「編集モード」を区別するフラグや状態を持つ
- 画面タイトル（「植物を追加」 or 「植物を編集」）と、ボタン文言（「登録する」 or 「更新する」）が、モードによって切り替わる

**前提条件**:
- フェーズ8〜9-4 が完了していること
- 既存の植物データ構造が維持されていること

## 完成の定義

- 1鉢の植物を登録できる
- 水やり判定が正しく動く（季節考慮あり）
- トラブル相談で対処法が見られる
- データがエクスポート/インポートできる
- iPhone Safariで快適に使える
- ホーム画面に追加してアプリのように使える

これらが満たされたら「最小バージョン完成」とする。
それ以降の拡張（写真機能の充実、植物データ追加、LLM連携など）は別フェーズで検討。